<% include ../partials/header.ejs %>
<!-- Name: COMP229 Midterm F2022 -->
<!-- Author:Rebecca Khidesheli -->
<!-- Student ID: 301229444 -->

<!-- MAIN CONTENT -->
<div class="container">
  <div class="row">
    <div class="col-md-offset-1 col-md-10">
      <h3>Product details</h3>
      <!-- ------------------------ -->
      <!-- FIX THE ADD BUTTON BELOW -->
      <!-- ------------------------ -->
      <a href="/products/add" class="btn btn-primary"
        ><i class="fa fa-plus"></i> Add a product</a
      >
      <br />
      <div class="table-responsive">
        <table class="table table-bordered table-striped table-hover">
          <thead>
            <tr>
              <th>Product id</th>
              <th class="text-center">Product name</th>
              <th class="text-center">Description</th>
              <th class="text-center">Price</th>
              <th class="text-center"></th>
            </tr>
          </thead>
          <tbody>
            <!-- Template Row -->
            <% for (let count = 0; count < products.length; count++) { %>
            <tr>
              <td><%= products[count].Productid %></td>
              <td class="text-center"><%= products[count].Productname %></td>
              <td class="text-center"><%= products[count].Description %></td>
              <td class="text-center"><%= products[count].Price %></td>

              <td class="text-center">
                <a href="/products/details/<%=products[count].id%>" class="btn btn-primary btn-sm"><i class="fa fa-pencil"></i> Edit</a>

              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
  
    <!-- ------------------------ -->
    <!-- ADD the DELETE BUTTON HERE -->
    <!-- ------------------------ -->
    <a onclick="Delete()" class="btn btn-warning btn-sm">
      <i class="fa fa-trash"></i> Delete</a>
   </main>

      <div class="col-md-offset-1 col-md-10 text-center">
        <ul class="pagination pagination-lg pager" id="myPager"></ul>
      </div>
    </div>
  </div>
</div>

<% include ../partials/footer.ejs %>
